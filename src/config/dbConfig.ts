import { connect } from "http2";

export const dbConfig = {
    host: 'localhost',
    port: 3306,
    user: 'root',
    password: 'Password@123',
    database: 'COPILOT_DEMO',
    connectionLimit: 10
};